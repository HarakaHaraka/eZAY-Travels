/**
 * Test stub for the `server-only` package, which throws unless imported from
 * a React Server Component. Vitest is not one, so it is aliased here (see
 * vitest.config.mts). Next still enforces the real constraint at build time.
 */
export {};
